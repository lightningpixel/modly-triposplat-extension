# SplatForge — shipped runtime modules used by the TripoSplat generator:
#   tsdf      : defect-driven extraction repair (Refined / Auto)
#   render/*  : multi-view splat/mesh renderers + render-supervised refine + auto-gate
#   normals   : vertex-normal smoothing for the GLB export
# The closed Phase A field-space optimizer (pipeline / optimize / fields / remesh /
# smooth / competition) is dev-only, untracked and kept locally — not imported here.
